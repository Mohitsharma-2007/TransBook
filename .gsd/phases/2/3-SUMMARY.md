# Plan 2.3 Summary
- Built `InvoicesScreen` with `DataTable2` for an organized dashboard.
- Built complex `NewInvoiceScreen` with dynamic row addition/removal.
- Integrated `GSTCalculator` and `IndianAmountWords` to dynamically compute totals based on Company's designated `InvoiceType`.
- Handled Riverpod async state for `companies` provider and corrected AppTheme mismatches.
- Registered `InvoicesScreen` as the default landing page in `main.dart` sidebar.
